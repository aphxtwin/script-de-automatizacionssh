# ================================
# GitHub SSH setup configuration
# ================================
# Copy this file to `.env` and fill in the values:
#   cp .env.example .env
# Then edit `.env` with your own data.

# --------------------------------
# GitHub personal access token
# --------------------------------
# Used by the script to call the GitHub API:
#   POST https://api.github.com/user/keys
# It must have at least:
#   - "admin:public_key" scope (for classic tokens), or
#   - "write:public_key" permission (for fine-grained tokens).
#
# How to get it:
#   1. Go to https://github.com/settings/tokens
#   2. Create a new token (fine‑grained or classic)
#   3. Enable the public key admin/write permission
#   4. Copy the token and paste it below
GITHUB_TOKEN="ghp_your_personal_access_token_here"

# --------------------------------
# GitHub username (optional)
# --------------------------------
# Your GitHub login/handle, e.g. "octocat".
# Currently not strictly required by the script, but useful
# for future extensions or logging.
GITHUB_USER="your_github_username_here"

# --------------------------------
# Email for SSH key comment (optional but recommended)
# --------------------------------
# This email is used as the comment in the SSH key:
#   ssh-keygen -t ed25519 -C "$GITHUB_EMAIL" ...
#
# Typically this is the same email registered on GitHub:
#   - Check at https://github.com/settings/emails
GITHUB_EMAIL="your_email@example.com"

# --------------------------------
# SSH key name (optional)
# --------------------------------
# File name (without path) for the SSH key pair that will be
# created under $HOME/.ssh
#
# Examples:
#   - "github"      -> key path: ~/.ssh/github
#   - "github_work" -> key path: ~/.ssh/github_work
#
# If not set, the script defaults to "github".
SSH_KEY_NAME="github"
